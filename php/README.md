# BlockCopy - PHP Full Stack Version

نظام إدارة المشاريع الذكي - نسخة PHP كاملة

## 📋 المتطلبات

- PHP 7.4 أو أعلى
- MySQL 5.7 أو أعلى
- Apache مع mod_rewrite أو nginx
- الامتدادات: PDO, PDO_MySQL, JSON, MBString

## 🚀 التثبيت

### 1. رفع الملفات

ارفع مجلد `php` بالكامل إلى خادمك:

```
public_html/
├── assets/
│   ├── css/
│   ├── js/
│   └── images/
├── api/ (من النسخة السابقة)
├── index.php
├── login.php
├── register.php
├── logout.php
├── projects.php
├── project.php
├── create-project.php
└── .htaccess
```

### 2. قاعدة البيانات

استورد ملف `api/database/setup.sql` إلى قاعدة البيانات.

### 3. إعدادات قاعدة البيانات

عدّل ملف `api/config/database.php`:

```php
define('DB_HOST', 'localhost');
define('DB_NAME', 'your_database_name');
define('DB_USER', 'your_database_user');
define('DB_PASS', 'your_database_password');
```

### 4. المستخدم الافتراضي

```
Email: admin@blockcopy.com
Password: admin123
```

## 📁 هيكل المشروع

```
php/
├── assets/
│   ├── css/
│   │   └── style.css          # الأنماط الرئيسية
│   ├── js/
│   │   └── app.js              # الوظائف الرئيسية
│   └── images/
│       └── default-avatar.png  # الصورة الافتراضية
├── api/                         # Backend API
│   ├── auth/                    # المصادقة
│   ├── projects/                # المشاريع
│   ├── blocks/                  # الأقسام
│   ├── files/                   # الملفات
│   ├── chat/                    # الدردشة
│   ├── config/                  # الإعدادات
│   ├── helpers/                 # الدوال المساعدة
│   └── database/                # قاعدة البيانات
├── index.php                    # الصفحة الرئيسية
├── login.php                    # تسجيل الدخول
├── register.php                 # إنشاء حساب
├── logout.php                   # تسجيل الخروج
├── projects.php                 # قائمة المشاريع
├── project.php                  # تفاصيل المشروع
├── create-project.php           # إنشاء مشروع
└── .htaccess                    # إعدادات Apache
```

## 🔧 إعدادات الخادم

### Apache

تأكد من تفعيل:
- mod_rewrite
- AllowOverride All

### nginx

أضف هذا إلى إعدادات nginx:

```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /path/to/php;
    index index.php;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.1-fpm.sock;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location /api {
        try_files $uri $uri/ /api/index.php?$query_string;
    }
}
```

## 📱 الصفحات

### الرئيسية
- `index.php` - لوحة التحكم مع إحصائيات سريعة

### المصادقة
- `login.php` - صفحة تسجيل الدخول
- `register.php` - صفحة إنشاء حساب
- `logout.php` - تسجيل الخروج

### المشاريع
- `projects.php` - قائمة جميع المشاريع
- `project.php` - تفاصيل مشروع واحد
- `create-project.php` - إنشاء مشروع جديد

## 🎨 التصميم

- تصميم عصري ومتجاوب
- دعم اللغة العربية (RTL)
- ألوان متناسقة
- سهل التخصيص

## 🔐 الأمان

- كلمات مرور مشفرة (bcrypt)
- حماية من CSRF
- التحقق من صحة البيانات
- حماية ملفات حساسة

## 🚀 المميزات

- ✅ إدارة المشاريع الكاملة
- ✅ نظام مصادقة متكامل
- ✅ تصميم متجاوب
- ✅ واجهة سهلة الاستخدام
- ✅ دعم اللغة العربية
- ✅ أقسام ومحتوى منظم
- ✅ إدارة الملفات
- ✅ نظام دردشة

## 📝 التطوير المستقبلي

- [ ] إدارة المستخدمين
- [ ] تصدير التقارير
- [ ] إشعارات
- [ ] تكامل مع خدمات خارجية
- [ ] مزيد من القوالب

## 🆘 الدعم

للدعم الفني، راجع:
- [`../README.md`](../README.md) - التوثيق العام
- [`api/README.md`](api/README.md) - توثيق API

## 📄 الترخيص

حقوق النشر © 2026 BlockCopy. جميع الحقوق محفوظة.
