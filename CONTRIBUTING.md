# دليل المساهمة

شكراً لاهتمامك بالمساهمة في مشروع مدير المشاريع الذكي! 🎉

## كيفية المساهمة

### الإبلاغ عن الأخطاء
- تحقق من عدم وجود issue مفتوح بنفس المشكلة
- وصف واضح للخطأ
- خطوات إعادة إنتاج الخطأ
- لقطات شاشة إن أمكن

### اقتراح ميزات جديدة
- وصف واضح للميزة
- الفائدة المتوقعة
- أمثلة على الاستخدام

### إرسال Pull Requests

1. **Fork** المشروع
2. **Clone** المشروع المحلي:
   ```bash
   git clone https://github.com/username/project-manager.git
   ```

3. **إنشاء فرع جديد**:
   ```bash
   git checkout -b feature/اسم-الميزة
   # أو
   git checkout -b fix/اسم-الإصلاح
   ```

4. **إجراء التغييرات**:
   - اتبع معايير الكود الحالية
   - أضف تعليقات واضحة
   - تأكد من عمل الاختبارات

5. **Commit** التغييرات:
   ```bash
   git add .
   git commit -m "وصف واضح للتغييرات"
   ```

6. **Push** إلى GitHub:
   ```bash
   git push origin feature/اسم-الميزة
   ```

7. **إنشاء Pull Request**:
   - اذهب إلى صفحة المشروع
   - اضغط "New Pull Request"
   - وصف التغييرات بوضوح

## معايير الكود

### TypeScript
- استخدم أنواع صريحة
- تجنب `any` قدر الإمكان
- وثّق الدوال المعقدة

### React
- استخدم Components وظيفية
- استخدم Hooks بشكل صحيح
- تجنب التكرار (DRY)

### CSS/Tailwind
- اتبع نظام التصميم الحالي
- استخدم الألوان من التكوين
- دعم RTL للعربية

### Git
- رسائل commit واضحة
- فرع واحد لكل ميزة
- تحديث الفرع بانتظام

## بنية المشروع

```
my-app/
├── app/              # Next.js App Router
│   ├── api/          # API Routes
│   ├── login/        # صفحة تسجيل الدخول
│   ├── register/     # صفحة التسجيل
│   └── project/      # صفحات المشروع
├── components/       # مكونات React
│   └── ui/          # مكونات shadcn/ui
├── lib/             # مكتبات ودوال
│   └── prisma.ts    # إعداد Prisma
├── prisma/          # مخطط قاعدة البيانات
├── types/           # أنواع TypeScript
└── public/          # ملفات ثابتة
```

## أسئلة؟

إذا كان لديك أي سؤال، لا تتردد في فتح issue.

شكراً! 🙏
